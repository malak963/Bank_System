<x-app-layout>
    <x-slot name="header"><div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p class="text-xs font-semibold uppercase text-emerald-700">{{ __('Credit Operations') }}</p><h2 class="mt-1 font-mono text-2xl font-semibold text-slate-950">{{ $loan->loan_reference }}</h2></div><a href="{{ route('loans.index') }}" class="text-sm font-semibold text-slate-600 hover:text-emerald-700">{{ __('Back to Loans') }}</a></div></x-slot>
    @php $statusClass = match ($loan->status->value) { 'active', 'disbursed' => 'bg-sky-50 text-sky-700 ring-sky-200', 'approved' => 'bg-emerald-50 text-emerald-700 ring-emerald-200', 'rejected', 'defaulted' => 'bg-red-50 text-red-700 ring-red-200', 'paid_off' => 'bg-green-50 text-green-700 ring-green-200', default => 'bg-amber-50 text-amber-700 ring-amber-200' }; @endphp
    <div class="mx-auto max-w-7xl space-y-6 px-4 py-8 sm:px-6 lg:px-8">
        @if (session('status'))<div class="rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-800">{{ session('status') }}</div>@endif
        @if ($errors->any())<div class="rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800">{{ $errors->first() }}</div>@endif
        <div class="grid grid-cols-1 gap-6 lg:grid-cols-3"><section class="rounded-lg bg-slate-950 p-6 text-white shadow-sm lg:col-span-2"><div class="flex items-start justify-between gap-4"><div><p class="text-xs font-semibold uppercase text-emerald-300">{{ __('Outstanding Principal') }}</p><p class="mt-3 text-4xl font-semibold">{{ number_format((float) $loan->outstanding_principal, 2) }} <span class="text-lg text-slate-400">{{ $loan->loanType?->currency }}</span></p></div><span class="inline-flex rounded-full px-3 py-1 text-xs font-semibold ring-1 ring-inset {{ $statusClass }}">{{ $loan->status->label() }}</span></div><div class="mt-8 grid grid-cols-1 gap-4 border-t border-slate-800 pt-5 sm:grid-cols-3"><div><p class="text-xs uppercase text-slate-500">{{ __('Total Payable') }}</p><p class="mt-1 text-lg font-semibold">{{ number_format((float) $loan->total_payable, 2) }}</p></div><div><p class="text-xs uppercase text-slate-500">{{ __('Total Paid') }}</p><p class="mt-1 text-lg font-semibold">{{ number_format((float) $loan->total_paid, 2) }}</p></div><div><p class="text-xs uppercase text-slate-500">{{ __('Next Payment') }}</p><p class="mt-1 text-lg font-semibold">{{ $loan->next_payment_date?->toDateString() ?? __('None') }}</p></div></div></section><section class="rounded-lg border border-slate-200 bg-white p-6 shadow-sm"><p class="text-xs font-semibold uppercase text-slate-500">{{ __('Borrower') }}</p><p class="mt-2 text-xl font-semibold text-slate-950">{{ $loan->customer?->full_name }}</p><p class="mt-1 text-sm text-slate-500">{{ $loan->customer?->customer_number }}</p><p class="mt-4 text-sm text-slate-600">{{ $loan->account?->account_number }} / {{ $loan->account?->accountType?->currency }}</p><a href="{{ route('customers.show', $loan->customer) }}" class="mt-5 inline-flex text-sm font-semibold text-emerald-700 hover:text-emerald-900">{{ __('View customer profile') }} &rarr;</a></section></div>
        <section class="rounded-lg border border-slate-200 bg-white p-6 shadow-sm"><h3 class="font-semibold text-slate-950">{{ __('Loan Details') }}</h3><dl class="mt-5 grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-4"><div><dt class="text-xs uppercase text-slate-500">{{ __('Loan Type') }}</dt><dd class="mt-1 font-medium text-slate-900">{{ $loan->loanType?->name }}</dd></div><div><dt class="text-xs uppercase text-slate-500">{{ __('Requested / Approved') }}</dt><dd class="mt-1 font-medium text-slate-900">{{ number_format((float) $loan->requested_amount, 2) }} / {{ $loan->approved_amount !== null ? number_format((float) $loan->approved_amount, 2) : __('Pending') }}</dd></div><div><dt class="text-xs uppercase text-slate-500">{{ __('Interest') }}</dt><dd class="mt-1 font-medium text-slate-900">{{ $loan->annual_interest_rate }}% {{ $loan->interest_method?->label() }}</dd></div><div><dt class="text-xs uppercase text-slate-500">{{ __('Term / Frequency') }}</dt><dd class="mt-1 font-medium text-slate-900">{{ $loan->term_months }} {{ __('months') }} / {{ $loan->repayment_frequency?->label() }}</dd></div><div><dt class="text-xs uppercase text-slate-500">{{ __('Applied') }}</dt><dd class="mt-1 font-medium text-slate-900">{{ $loan->application_date?->toDateString() }}</dd></div><div><dt class="text-xs uppercase text-slate-500">{{ __('Approved At') }}</dt><dd class="mt-1 font-medium text-slate-900">{{ $loan->approved_at?->format('Y-m-d H:i') ?? __('Not approved') }}</dd></div><div><dt class="text-xs uppercase text-slate-500">{{ __('Disbursed At') }}</dt><dd class="mt-1 font-medium text-slate-900">{{ $loan->disbursed_at?->format('Y-m-d H:i') ?? __('Not disbursed') }}</dd></div><div><dt class="text-xs uppercase text-slate-500">{{ __('Approved By') }}</dt><dd class="mt-1 font-medium text-slate-900">{{ $loan->approver?->name ?? __('Not approved') }}</dd></div></dl>@if ($loan->purpose)<div class="mt-5 rounded-lg bg-slate-50 p-4 text-sm text-slate-700"><span class="font-semibold">{{ __('Purpose') }}:</span> {{ $loan->purpose }}</div>@endif @if ($loan->rejection_reason)<div class="mt-5 rounded-lg border border-red-200 bg-red-50 p-4 text-sm text-red-900"><span class="font-semibold">{{ __('Rejection reason') }}:</span> {{ $loan->rejection_reason }}</div>@endif</section>
        @if ($loan->status->value === 'pending' || $loan->status->value === 'under_review')<section class="grid grid-cols-1 gap-6 lg:grid-cols-2"><form method="POST" action="{{ route('loans.approve', $loan) }}" class="rounded-lg border border-emerald-200 bg-emerald-50 p-6">@csrf<h3 class="font-semibold text-emerald-950">{{ __('Approve Application') }}</h3><div class="mt-4 grid grid-cols-1 gap-4 sm:grid-cols-2"><div><x-input-label for="approved_amount" :value="__('Approved Amount')" /><x-text-input id="approved_amount" name="approved_amount" type="number" min="0.01" step="0.01" class="mt-1 block w-full bg-white" :value="old('approved_amount', $loan->requested_amount)" /></div><div><x-input-label for="first_payment_date" :value="__('First Payment Date')" /><x-text-input id="first_payment_date" name="first_payment_date" type="date" class="mt-1 block w-full bg-white" :value="old('first_payment_date', now()->addMonth()->toDateString())" /></div></div><button class="mt-5 rounded-lg bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white hover:bg-emerald-800">{{ __('Approve and Generate Schedule') }}</button></form><form method="POST" action="{{ route('loans.reject', $loan) }}" class="rounded-lg border border-red-200 bg-red-50 p-6">@csrf<h3 class="font-semibold text-red-950">{{ __('Reject Application') }}</h3><textarea name="rejection_reason" rows="3" class="mt-4 block w-full rounded-lg border-red-200 bg-white text-sm shadow-sm focus:border-red-500 focus:ring-red-500" placeholder="{{ __('Reason is required') }}">{{ old('rejection_reason') }}</textarea><button class="mt-5 rounded-lg border border-red-300 px-4 py-2.5 text-sm font-semibold text-red-700 hover:bg-red-100">{{ __('Reject Loan') }}</button></form></section>@endif
        @if ($loan->status->value === 'approved')<section class="rounded-lg border border-sky-200 bg-sky-50 p-6"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h3 class="font-semibold text-sky-950">{{ __('Ready for Disbursement') }}</h3><p class="text-sm text-sky-800">{{ __('The approved amount will be credited to the linked open account.') }}</p></div><form method="POST" action="{{ route('loans.disburse', $loan) }}">@csrf<button class="rounded-lg bg-sky-700 px-4 py-2.5 text-sm font-semibold text-white hover:bg-sky-800">{{ __('Disburse Loan') }}</button></form></div></section>@endif
        @if (in_array($loan->status->value, ['active', 'disbursed'], true))<section class="rounded-lg border border-amber-200 bg-amber-50 p-6"><h3 class="font-semibold text-amber-950">{{ __('Record Repayment') }}</h3><form method="POST" action="{{ route('loans.payments.store', $loan) }}" class="mt-4 grid grid-cols-1 gap-4 lg:grid-cols-12">@csrf<div class="lg:col-span-3"><x-input-label for="amount" :value="__('Payment Amount')" /><x-text-input id="amount" name="amount" type="number" min="0.01" max="{{ $loan->outstanding_principal + $loan->outstanding_interest }}" step="0.01" class="mt-1 block w-full bg-white" required /></div><div class="lg:col-span-3"><x-input-label for="payment_method" :value="__('Method')" /><select id="payment_method" name="payment_method" class="mt-1 block w-full rounded-lg border-slate-300 bg-white shadow-sm" required>@foreach ($paymentMethods as $method)<option value="{{ $method->value }}">{{ $method->label() }}</option>@endforeach</select></div><div class="lg:col-span-3"><x-input-label for="paid_at" :value="__('Paid At')" /><x-text-input id="paid_at" name="paid_at" type="datetime-local" class="mt-1 block w-full bg-white" :value="old('paid_at', now()->format('Y-m-d\TH:i'))" /></div><div class="flex items-end lg:col-span-3"><button class="w-full rounded-lg bg-amber-700 px-4 py-2.5 text-sm font-semibold text-white hover:bg-amber-800">{{ __('Record Payment') }}</button></div><div class="lg:col-span-12"><x-input-label for="notes" :value="__('Notes')" /><textarea id="notes" name="notes" rows="2" class="mt-1 block w-full rounded-lg border-slate-300 bg-white shadow-sm">{{ old('notes') }}</textarea></div></form></section>@endif
        <section class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-200 px-5 py-4"><h3 class="font-semibold text-slate-950">{{ __('Repayment Schedule') }}</h3><p class="text-sm text-slate-500">{{ __('Principal and interest are tracked separately for each installment.') }}</p></div><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200"><thead class="bg-slate-50"><tr><th class="px-5 py-3 text-left text-xs font-semibold uppercase text-slate-500">#</th><th class="px-5 py-3 text-left text-xs font-semibold uppercase text-slate-500">{{ __('Due Date') }}</th><th class="px-5 py-3 text-right text-xs font-semibold uppercase text-slate-500">{{ __('Principal') }}</th><th class="px-5 py-3 text-right text-xs font-semibold uppercase text-slate-500">{{ __('Interest') }}</th><th class="px-5 py-3 text-right text-xs font-semibold uppercase text-slate-500">{{ __('Due') }}</th><th class="px-5 py-3 text-right text-xs font-semibold uppercase text-slate-500">{{ __('Paid') }}</th><th class="px-5 py-3 text-left text-xs font-semibold uppercase text-slate-500">{{ __('Status') }}</th></tr></thead><tbody class="divide-y divide-slate-100">
            @forelse ($loan->installments as $installment) @php $installmentStatus = $installment->displayStatus(); @endphp<tr><td class="px-5 py-3 text-sm font-semibold text-slate-700">{{ $installment->installment_number }}</td><td class="px-5 py-3 text-sm text-slate-700">{{ $installment->due_date?->toDateString() }}</td><td class="px-5 py-3 text-right text-sm text-slate-700">{{ number_format((float) $installment->principal_amount, 2) }}</td><td class="px-5 py-3 text-right text-sm text-slate-700">{{ number_format((float) $installment->interest_amount, 2) }}</td><td class="px-5 py-3 text-right text-sm font-semibold text-slate-900">{{ number_format((float) $installment->amount_due, 2) }}</td><td class="px-5 py-3 text-right text-sm text-slate-700">{{ number_format((float) $installment->amount_paid, 2) }}</td><td class="px-5 py-3 text-sm"><span class="rounded-full px-2.5 py-1 text-xs font-semibold ring-1 ring-inset {{ $installmentStatus->value === 'paid' ? 'bg-emerald-50 text-emerald-700 ring-emerald-200' : ($installmentStatus->value === 'overdue' ? 'bg-red-50 text-red-700 ring-red-200' : 'bg-amber-50 text-amber-700 ring-amber-200') }}">{{ $installmentStatus->label() }}</span></td></tr>@empty<tr><td colspan="7" class="px-6 py-12 text-center text-sm text-slate-500">{{ __('No installment schedule has been generated yet.') }}</td></tr>@endforelse
        </tbody></table></div></section>
        <section class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-200 px-5 py-4"><h3 class="font-semibold text-slate-950">{{ __('Payment History') }}</h3></div><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200"><thead class="bg-slate-50"><tr><th class="px-5 py-3 text-left text-xs font-semibold uppercase text-slate-500">{{ __('Reference') }}</th><th class="px-5 py-3 text-left text-xs font-semibold uppercase text-slate-500">{{ __('Date') }}</th><th class="px-5 py-3 text-right text-xs font-semibold uppercase text-slate-500">{{ __('Amount') }}</th><th class="px-5 py-3 text-right text-xs font-semibold uppercase text-slate-500">{{ __('Principal') }}</th><th class="px-5 py-3 text-right text-xs font-semibold uppercase text-slate-500">{{ __('Interest') }}</th><th class="px-5 py-3 text-left text-xs font-semibold uppercase text-slate-500">{{ __('Method') }}</th></tr></thead><tbody class="divide-y divide-slate-100">
            @forelse ($loan->payments as $payment)<tr><td class="px-5 py-3 font-mono text-sm text-slate-800">{{ $payment->payment_reference }}</td><td class="px-5 py-3 text-sm text-slate-700">{{ $payment->paid_at?->format('Y-m-d H:i') }}</td><td class="px-5 py-3 text-right text-sm font-semibold text-slate-900">{{ number_format((float) $payment->amount, 2) }}</td><td class="px-5 py-3 text-right text-sm text-slate-700">{{ number_format((float) $payment->principal_amount, 2) }}</td><td class="px-5 py-3 text-right text-sm text-slate-700">{{ number_format((float) $payment->interest_amount, 2) }}</td><td class="px-5 py-3 text-sm text-slate-700">{{ $payment->payment_method?->label() }}</td></tr>@empty<tr><td colspan="6" class="px-6 py-12 text-center text-sm text-slate-500">{{ __('No payments recorded.') }}</td></tr>@endforelse
        </tbody></table></div></section>
    </div>
</x-app-layout>
